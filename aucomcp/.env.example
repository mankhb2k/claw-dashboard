PORT=8080
HOST=0.0.0.0

# Shared secret with aucobot api (sign/verify project MCP tokens + internal API)
MCP_SERVICE_SECRET=change-me-mcp-service-secret

# AucoBot API base URL (include /api prefix is added by client)
AUCOBOT_INTERNAL_API_URL=http://localhost:3001
