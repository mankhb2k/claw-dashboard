# Workflow (redirect)

**Single source of truth:** [`workflow.md`](../../workflow.md) tại repo root — luồng vận hành AucoBot (OSS/Cloud), sync DB ↔ volume, chat WS proxy, channels API.

File này không nhân bản nội dung; chỉ giữ link cho người đọc từ `aucobot/docs/`.
