# @aucobot/runtime-contracts

Types and interfaces for OSS vs Cloud runtime wiring.

- `GatewayEndpoint`, `ProjectGatewayFields`
- `GatewayEndpointResolver`, `PlanGuard` (stub for cloud)
- `RuntimeMode`, `isOssRuntime()`

No NestJS / Prisma dependencies.
