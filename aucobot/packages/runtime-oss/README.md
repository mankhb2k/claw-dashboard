# @aucobot/runtime-oss

OSS runtime: shared gateway at `OPENCLAW_GATEWAY_URL` (default `:18789`).

- `resolveOssGatewayEndpoint`, `ossGatewayEndpointResolver`
- `waitForGatewayHealth` — poll `/healthz`

Cloud spawn/Docker: `cloud/packages/fleet` (Phase 4).
