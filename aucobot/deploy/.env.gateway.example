# Copy to deploy/.env.gateway — used by pnpm dev:runtime
# OPENCLAW_GATEWAY_TOKEN must match aucobot/.env
OPENCLAW_IMAGE=alpine/openclaw:latest
OPENCLAW_GATEWAY_TOKEN=change-me-gateway-token
OSS_PROJECT_ID=
