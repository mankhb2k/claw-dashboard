# Gateway dev — used by pnpm dev:runtime (must match aucobot/.env OPENCLAW_GATEWAY_TOKEN)
OPENCLAW_IMAGE=alpine/openclaw:latest
OPENCLAW_GATEWAY_TOKEN=change-me-gateway-token
# Pin active OSS project (gateway entrypoint picks first folder if empty)
OSS_PROJECT_ID=cmqhko9s20001bwvhdnyfc7kd
