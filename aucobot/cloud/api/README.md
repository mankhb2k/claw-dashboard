# @aucobot/cloud-api (placeholder)

NestJS control plane **hosted Cloud** — Phase 4.

- Import `@aucobot/control-plane-core`
- Wire `DockerPerProjectProvisioner` từ `@aucobot-cloud/fleet`
- Wire `StripePlanGuard` từ `@aucobot-cloud/quota`

**Chưa implement.** OSS: dùng `apps/api`.
